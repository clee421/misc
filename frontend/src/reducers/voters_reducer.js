import merge from 'lodash/merge';


const VoterReducer = (state = {}, action) => {
  Object.freeze(state);

  switch (action.type) {

    default:
      return state;
  }
};

export default VoterReducer;