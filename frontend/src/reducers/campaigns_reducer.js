import merge from 'lodash/merge';


const CampaignReducer = (state = {}, action) => {
  Object.freeze(state);

  switch (action.type) {

    default:
      return state;
  }
};

export default CampaignReducer;